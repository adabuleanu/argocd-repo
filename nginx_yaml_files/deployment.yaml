apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    apps: nginx
  name: nginx-deployment
specs:
  replicas: 1
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        apps: nginx
    specs:
      containers:
      - image: nginx
        name: nginx
